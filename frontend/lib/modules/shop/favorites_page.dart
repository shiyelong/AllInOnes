import 'package:flutter/material.dart';

/// 我的收藏页面（移动端）
class ShopFavoritesPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('我的收藏')),
      body: Center(child: Text('我的收藏功能敬请期待')), 
    );
  }
}
