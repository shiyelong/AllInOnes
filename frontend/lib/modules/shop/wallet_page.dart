import 'package:flutter/material.dart';

/// 我的钱包页面（移动端）
class ShopWalletPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('我的钱包')),
      body: Center(child: Text('我的钱包功能敬请期待')), 
    );
  }
}
