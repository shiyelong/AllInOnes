import 'package:flutter/material.dart';

/// 收货地址页面（移动端）
class ShopAddressPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('收货地址')),
      body: Center(child: Text('收货地址功能敬请期待')), 
    );
  }
}
