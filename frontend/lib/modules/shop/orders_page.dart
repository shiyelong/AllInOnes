import 'package:flutter/material.dart';

/// 我的订单页面（移动端）
class ShopOrdersPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('我的订单')),
      body: Center(child: Text('我的订单功能敬请期待')), 
    );
  }
}
