import 'package:flutter/material.dart';

class MiniAppList extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    // TODO: 小程序列表实现
    return ListView(
      children: [
        ListTile(title: Text('小程序A')), 
      ],
    );
  }
}
