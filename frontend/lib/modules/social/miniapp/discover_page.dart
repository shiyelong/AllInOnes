import 'package:flutter/material.dart';

/// 发现页面（假页面/骨架）
class DiscoverPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('发现')),
      body: Center(child: Text('发现功能敬请期待')), 
    );
  }
}
