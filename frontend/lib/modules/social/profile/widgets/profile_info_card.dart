import 'package:flutter/material.dart';

class ProfileInfoCard extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    // TODO: 个人信息卡片实现
    return Card(
      child: ListTile(title: Text('用户名')), 
    );
  }
}
