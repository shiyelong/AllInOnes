import 'package:flutter/material.dart';

/// 收藏页面（假页面/骨架）
class FavoritesPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('收藏')),
      body: Center(child: Text('收藏功能敬请期待')), 
    );
  }
}
