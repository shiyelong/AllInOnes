import 'package:flutter/material.dart';

/// 订单页面（假页面/骨架）
class OrdersPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('订单')),
      body: Center(child: Text('订单功能敬请期待')), 
    );
  }
}
