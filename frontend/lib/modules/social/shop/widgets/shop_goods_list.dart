import 'package:flutter/material.dart';

class ShopGoodsList extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    // TODO: 商品列表实现
    return ListView(
      children: [
        ListTile(title: Text('商品A')), 
      ],
    );
  }
}
