import 'package:flutter/material.dart';

/// 钱包页面（假页面/骨架）
class WalletPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('钱包')),
      body: Center(child: Text('钱包功能敬请期待')), 
    );
  }
}
