import 'package:flutter/material.dart';

/// AI助手页面（假页面/骨架）
class AiAssistantPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('AI助手')),
      body: Center(child: Text('AI助手功能敬请期待')), 
    );
  }
}
