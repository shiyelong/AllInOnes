import 'package:flutter/material.dart';

class AiToolsList extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    // TODO: AI 工具/模型列表实现
    return ListView(
      children: [
        ListTile(title: Text('AI 工具A')), 
      ],
    );
  }
}
