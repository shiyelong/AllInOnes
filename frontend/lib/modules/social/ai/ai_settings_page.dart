import 'package:flutter/material.dart';

/// AI设置页面（假页面/骨架）
class AiSettingsPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('AI设置')),
      body: Center(child: Text('AI设置功能敬请期待')), 
    );
  }
}
