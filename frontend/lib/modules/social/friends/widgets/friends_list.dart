import 'package:flutter/material.dart';

class FriendsList extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    // TODO: 好友列表实现
    return ListView(
      children: [
        ListTile(title: Text('好友A')), 
      ],
    );
  }
}
