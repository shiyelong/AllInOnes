import 'package:flutter/material.dart';

/// 达人页面（假页面/骨架）
class StarsPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('达人')),
      body: Center(child: Text('达人功能敬请期待')), 
    );
  }
}
