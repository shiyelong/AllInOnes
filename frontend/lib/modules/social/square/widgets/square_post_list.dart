import 'package:flutter/material.dart';

class SquarePostList extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    // TODO: 广场帖子流实现
    return ListView(
      children: [
        ListTile(title: Text('帖子标题')), 
      ],
    );
  }
}
