import 'package:flutter/material.dart';

/// 排行页面（假页面/骨架）
class RankingPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('排行')),
      body: Center(child: Text('排行功能敬请期待')), 
    );
  }
}
