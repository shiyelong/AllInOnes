import 'package:flutter/material.dart';

class GameList extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    // TODO: 游戏大厅列表实现
    return ListView(
      children: [
        ListTile(title: Text('游戏A')), 
      ],
    );
  }
}
